class Solution {
public:
    bool containsDuplicate(vector<int>& nums) {
        set<int> sett ;
        int s ;
         int n = nums.size() ;
        for ( int i=0 ; i <n ; i++ ){
            sett.insert(nums[i] ) ;
        }
        s= sett.size() ; 
        return (s != n) ; 
        
    }
};
